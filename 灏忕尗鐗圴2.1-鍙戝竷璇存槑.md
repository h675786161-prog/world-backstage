# 世界背面 · 小猫版 V2.1 🐈

玲七正式搬进世界背面啦。

这次不是简单“加一只猫”，而是整个插件的使用方式重新长了一遍。玲七现在会帮你答疑、看状态、整理记忆、处理低风险设置、判断问题归属，还会在自己确实解决不了、而且真的像插件问题时，写一张给妈妈的小纸条。

## V2.1 主要变化

- **玲七全能小猫管家**：聊天、答疑、诊断、低风险代办、记忆整理、导演小纸条、问题分流。
- **更聪明的问题分流**：用户自己能解决的先教；API / 中转 / 第三方问题先分流；真正像插件 bug 才找妈妈。
- **正文注入独立管理**：时间、背景、人物、暗流、回声、事实、记忆、舆情都能分别决定是否递给正文。
- **不注入 ≠ 不运行**：镜头没看到的世界仍然继续生活。
- **世界推演更安全**：输出被截断、空回、失败时不提交半份人物 / 事件 / 记忆 / 世界时间。
- **后台任务安全排队**：记忆整理和世界推演不会互相抢写。
- **玲七动作跟着回复走**：取消随机幻灯片；猫咪换动作不打断输入，也不会把展开的小纸条合上。
- **设置页重新整理**：常用 / 正文注入 / 连接与模型 / 高级维护四个入口，解释改成玲七的小猫视角。
- **旧数据安全优先**：事实权威、删除记录、恢复点、异步任务失效等保护继续保留。

## 版本

对外版本：**小猫版 V2.1**  
技术版本：`2.1.0`

> 世界背面不是围着正文运转的附属插件。正文只是镜头；世界本身一直都在背后继续。


## 后台 API 调度修复

- 同一独立接口路线不再让世界推演、记忆、舆情等任务并发撞车。
- 新正文不会再把已经发出的低优先级请求掐掉后立刻重发，避免上游实际未停止时继续叠请求。
- 主推演会顺手生成当前轮的 L0 记忆摘要，正常情况下不再让记忆模块为同一轮正文再打一遍 API。
- 公共影响与舆情改为顺序后处理，不再由多个 timer 重复唤醒。
- 诊断信息现在会显示最近请求频率与任务分类，429 更容易定位。


## 酒馆已保存 API 方案

- 世界背面现在可以直接读取 SillyTavern Connection Manager 已保存的 API 方案。
- 只保存方案 ID，不复制 Key、URL、模型或 Secret；酒馆里的方案更新后，世界背面下次调用直接读取新值。
- 方案被删除或不可用时会明确停止并报错，不会静默退回其他接口。
- 默认连接与世界推演 / 人物观测 / 记忆 / 舆情的模块分流都可以选择酒馆方案。
- 普通独立 OpenAI 兼容接口的「经酒馆转发」同步改为使用 SillyTavern Custom OpenAI-compatible 通道，避免把任意兼容站误当 OpenAI Reverse Proxy。


## UI 与伏笔整理

- 修复推演成功后偶发“悬浮球有黄点，但点开还是上一份内容”的旧快照问题：面板现在按 world revision 自检并刷新。
- 悬浮球可开启半球贴边收纳：正好隐藏一半、显示一半，不需要在“完全显示 / 完全隐藏”之间二选一。
- 全局设置的标题、关闭按钮和四个分类页签会一起固定在设置面板顶部。
- 伏笔增加「已回收 / 已归档」生命周期视图；归档不改写 resolved / discarded 的真实历史。
- 伏笔支持人物 / 事件 / 地点多关联和筛选，不要求唯一人物归属。
- 手动删除记忆新增 tombstone 防诈尸；state schema 升级到 23，旧数据升级前会沿用恢复点保护。
