{
  "name": "world-backstage",
  "private": true,
  "version": "1.5.3",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check index.js && node --check core.js && node --check ui.js && node --check api.js && node --check worldbook.js && node --check public-opinion.js"
  }
}
