## 2.3.0-alpha.6（小猫版 V2.3 · 全角色即时观测）

- 「看看 TA 在做什么」开放给全部非 user 角色，不再因为人物已经出现在本轮镜头中而隐藏或拒绝观测。
- 界面与执行层使用同一条排除规则；旧数据中仅通过酒馆用户名识别出的 user 角色也不会误开放观测。
- 继续保持逐个观测和按人物缓存：打开人物板块不会自动批量请求 API，也不会因为角色数量增加制造并发观测。
- 人物即时观测仍不推进世界时间、不修改人物状态、不直接写入正文；自然显露继续由用户单独开启。
- 版本号更新为 `2.3.0-alpha.6`，数据 schema 保持不变。

## 2.3.0-alpha.5（小猫版 V2.3 · 玲七全能管家）

- 新增统一玲七技能注册表，集中登记查询、诊断、控制、整理能力及风险/确认等级；动作白名单由注册表派生。
- 新增本地世界总览、人物清单与状态、活跃暗流、记忆状态、设置概览等查询，不必为常见盘点额外请求模型。
- 新增世界推演与人物停滞诊断：核对总开关、自动运行、后台连接、活动任务、排队、最新正文快照与真实错误；证据不足时明确拒绝硬猜。
- 新增玲七聊天搜索，可按明确关键词找回真实命中的历史记录，不改动聊天或长期记忆。
- 设置问答升级为分步骤引导：显示当前值、准确界面路径、选项区别、适用场景与安全默认建议；可代办的安全开关会明确提示，API Key、URL、模型与高风险数据仍不允许直接修改。
- 新增“你会什么”技能清单入口；常用管家能力不再依赖用户记住按钮名称。
- 版本号更新为 `2.3.0-alpha.5`，数据 schema 保持不变。

## 2.3.0-alpha.4（小猫版 V2.3 · 世界传播链）

- 玲七新增聊天记录内部管理：用户可直接用自然语言要求清空全部、删除最近若干条/轮、按今天/昨天/前天、按某句之前/之后、从某句到某句或按明确主题删除一段玲七聊天。
- 删除动作必须先由插件本地定位实际记录，再弹出条数、首尾预览和二次确认；找不到或明显歧义时拒绝猜测。删除玲七聊天默认不删除长期记忆、世界状态或小纸条。
- 玲七聊天保存上限由 36 条扩到 800 条；界面只渲染最近 120 条，较早记录继续保留在本地供玲七搜索/删除，避免长聊天把聊天区 DOM 撑爆。
- 新闻继续只报道已经成立的公开事件，但真实后果会沿地点、行程、工作、组织、资源与消息渠道进入世界状态。
- 公共事件影响人物前新增物理链、信息链、因果链校验；公开不再等于所有 NPC 自动知情。
- 同一公共变化波及多人时先结算共享后果，再同步人物状态与各自认知，避免拆成互相矛盾的个人小剧场。
- 社会评价必须具备目击、物证、公告、媒体或有效传闻等可见性基础；圈层舆论不会反向覆盖客观事实。
- 舆情设置说明继续保留小猫语气，并直接说明新闻到人物之间的传播路径。

## 2.3.0-alpha.3（小猫版 V2.3 · 群体生活与玲七代办）

- 玲七改用与主界面、此刻页和悬浮球相同的权威正文同步快照，不再从旧 `pendingSync` 猜测最新正文是否已推演；同时补齐 V2.3 公共世界轮转、三小时节点、同刻去重与新闻因果传播知识。
- 玲七新增四类安全代办：推演最新正文、巡查公共世界并刷新舆情、把人物排到下一轮优先、立即补一次人物近况。下一轮优先不调用 API；其余三类会先显示确认卡，确认后才复用现有安全队列与合批链路。
- 待确认动作绑定当前聊天与上下文版本；切换聊天、重置或状态失效时会自动清掉，连续点击也不会重复启动。公共巡查遇到核心任务占用会明确显示已排队，不再提前声称已经完成。
- 人物推演增加“群体生活协调”：按精确地点/时间、共同事件、组织工作、关系、约定和日程判断自然交集；真实多人互动必须给参与者留下对称状态并共用同一事件，预算不足时不得假装互动已经完成。
- 群体生活继续遵守人物知识隔离：交谈、消息、目击和共同工作只更新真实接收者的认知路径；同地、亲密、同事或同组织都不等于共享后台全知。该能力复用现有人物合批推演，不新增 API 轮次。
- 正文注入来源名称与侧栏板块对齐，明确标示来自「此刻 / 人物 / 暗流 / 回声 / 记忆 / 舆情」；模块内状态卡沿用同一套名字，不再出现两套互不对应的术语。
- 设置页普通说明继续统一为玲七的小猫语气；技术标签、数据安全边界和危险操作警告保留清楚直白，不为了卖萌牺牲可读性。
- 数据结构仍为 schema 24；本版不改写既有正文历史，不新增独立定时器。

## 2.3.0-alpha.2（小猫版 V2.3 · 推演状态修复）

- 正文推演状态统一使用当前消息、滑动分支、内容指纹和提交快照判断；不再出现主窗口说“已完成”，悬浮球却继续拿旧 `pendingSync` 标记点亮黄点的矛盾。
- 主界面状态条、此刻页世界脉搏、悬浮球黄点与小气泡现在读取同一份“最新正文是否已推演”结果；打开面板后，已读的世界更新提示会立即熄灭。
- 黄色点只表示正文待推演、失败或需要处理；普通未读世界变化改用主题色小点，避免“推演完成”旁边仍亮黄点造成误解。
- 小气泡确认存在未推演正文且当前没有正在运行或排队的世界推演时，显示“推演”按钮；失败时显示“重新推演”，点击后复用原有安全队列，不并发写世界状态。
- 手机端将这条可操作提醒改成带“推演 / 稍后”的轻量弹窗；“稍后”只安静当前这轮，下一轮新正文仍会重新提醒。
- 保留 `2.3.0-alpha.1` 的公共世界轮转、新闻因果传播、人物第一人称人格锁与小猫语气；数据结构仍为 schema 24。

## 2.3.0-alpha.1（小猫版 V2.3 · 公共世界轮转体验版）

- 新增持久化“世界覆盖账本”（schema 24）：记录镜头外地区、宏观领域、已知地点与用户侧重点最近一次被世界推演检查的世界时刻。
- 正常正文推演不增加 API 次数，顺手轮转检查 2 个长期未照看的世界范围；活跃模式最多检查 3 个。检查不是事件配额，没有自然变化时允许保持安静。
- “巡一圈并刷新”在首次升级、世界时间距上次巡查至少 180 分钟或用户改变世界侧重点时，先执行一次公共世界巡查，再结算公开影响并整理新闻/论坛。
- 同一世界时刻、同一世界侧重点下反复点击不会重复巡查或无限制造事件。
- 公开来源、世界时间节点与覆盖巡查结果都没有变化时，按钮直接沿用当前舆情，不再只为改写措辞调用一次模型。
- 到期公共巡查会在同一份世界请求中结算已有/新公开事件的直接客观后果和真实获知渠道；一次刷新最多使用“公共世界 + 舆情”两份串行请求，不再紧接着另打一份公共影响请求。
- 公共世界巡查优先普通天气、交通、商业、活动、行业、设施、地方政策、治安与文化娱乐，不要求与主角有关，也不强制制造灾难或大事件。
- 舆情页显示最近一次“镜头外巡查”的世界时间和最近巡过的范围，按钮文案与提示同步说明真实行为。
- `2.2.2` 保持为稳定退路；本体验版不新增独立定时器，不修改已发生正文历史。

## 2.2.2（小猫版 V2.2 · 归档折叠布局修复）

- 修复玲七「小盒子里」误继承 20×16 像素小箭头详情样式，导致标题、数量和箭头被压成竖排重叠的问题。
- 归档盒子与聊天内的简易详情折叠彻底分离；标题保持完整横排，默认收起，展开后纸条内容在下一行正常显示，展开状态仍可跨界面刷新保持。

## 2.2.1（小猫版 V2.2 · 综合优化修复）

- 补发 V2.2 综合优化修复版的正确技术版本号；同步扩展清单、运行时常量、包信息与发布说明，确保酒馆能识别这是高于 `2.2.0` 的升级。
- 包含人物观测人格锁与知识隔离、贴边隐藏自弹修复、世界新闻因果传播、玲七归档折叠及设置界面精简。

## 2.2.0（小猫版 V2.2 · 正式发布）

- 修复「自定义推演要求」在世界脉搏 / 公共世界链路中丢失的问题：世界新闻地区、行业与主题侧重点现在会同时传给镜头外公共世界和舆情选题，不再被最新剧情里的相近事件敷衍替代。
- 新闻与论坛在候选覆盖多个地点 / 行业 / 事件线时会主动分散选题，不再因为某条事件更靠近当前人物就把整版内容挤回当前剧情；仍只消费真实公开事件，不凭空捏造正史。
- 补齐公开事件 → 人物认知 → 正文闭环：手动检查舆情也会先结算既有公开事件的传播影响；人物通过新闻、公告、职业渠道或现场感知获得的冻结认知版本会进入正文权威提示，但不会泄漏后台真相或让所有人自动全知。
- 公共影响结算会优先带入源事件参与者和当前位于受影响地点的人物，天气、封路、停电、行业公告等可自然改变相关人物的认知、行程与行动；无接触渠道的人仍保持不知情。
- 玲七「小盒子里」改为默认收起的原生折叠区，展开状态在界面刷新时保持；只有归档纸条时不再把整列撑满。
- 模块设置继续去掉重复搬家说明与常驻教程卡片，短说明改为轻量文字；Token / 等待说明收短，保留必要边界与玲七语气。
- 「看看 TA 在做什么」增加第一人称人格锁：先承接人物已结算状态、认知、人格、表达能力与生活惯性，不再为了戏剧性把寡言/直接的人物突然写成散文家。
- 人物即时观测不再默认围着玩家转；平淡、重复、休息、发呆和什么都没发生都是合法状态，不为凑字数强行制造小剧情。
- 修复人物观测认知泄漏：角色所知事件只携带当时获得的冻结版本，不再附带后台事件的实时地点和结束状态。
- 修复观测缓存失效不完整：人格锚点、背景/关系、说话习惯、行为边界、内心连续线索、人物认知与身心/资源状态变化后，不会继续复用旧观测。
- 修复 30 条观测缓存已满时，强制刷新一条较旧缓存可能又被当场淘汰的顺序问题。
- 修复开启「贴边隐藏」后悬浮球每隔约 1.8 秒又自己弹回完整位置的问题：界面自愈检查和可视区域变化现在都会保持半隐藏停靠，不再用完整可见坐标覆盖它。
- 修复暗流生命周期的关键歧义：主推演明确要求同一条未结束暗流的推进、受阻、等待条件、线索变化等必须使用 `events_update` 更新原 ID；只有真正独立的新因果或已结算事件长出的后续事件才允许 `events_create`。
- 修复 `events_create` 命中旧事件后可能把旧事件 ID 替换成模型新 ID 的问题；旧事件身份现在保持稳定，避免关联引用断裂。
- 活跃事件超过详细上下文容量时，新增轻量 `event_index` 身份索引；模型即使看不到全部详细内容，也仍能看到其余活跃暗流的 ID / 标题 / 地点 / 状态 / 参与者 / 上游关系，降低重复创建。
- 记忆工具区合并重排：统计、新增记忆 / 伏笔、生命周期筛选、伏笔关联、搜索与数量收进同一块面板，减少双层卡片与大面积空白。
- 独立接口按钮区、停止全部后台任务按钮与高级维护卡片继续做样式收口；维护说明压缩为短句并保留玲七语气，危险操作仍保留必要警告。
- 不新增暗流硬数量上限；先修正“更新被误当新建”的根因，避免为了控数量粗暴吃掉仍在运行的世界事实。
- 后台 NPC 单轮预算移除 12 人硬上限：自定义输入不再带 `max=12`，设置与推演链路不再把高于 12 的值偷偷裁回；默认仍为 4，界面给出“推荐 2～8、单轮尽量不超过 12”的软提醒。
- 人物详情新增「补一下近况」：手动指定一个后台人物做一次生活结算，复用世界脉搏 API 链路、`elapsed_minutes=0`，不推进世界时间；提交前本地强制只接受指定人物，并只允许与其直接关联的事件 / 世界事实跟随更新。
- 人物详情新增「下一轮优先」：不立刻调用 API，只把该人物排入下一次后台结算必做队列；成功结算后自动清除优先标记。
- 后台人物增加防饿死保底：连续超过自身 3 个生活周期未完成结算时，即使未开启“强化后台人物推演”也会进入必做队列；普通到期仍沿用原预算与调度逻辑。
- 当前技术版本更新为 `2.2.0`；对外版本名更新为「小猫版 V2.2」。

## 2.1.0（小猫版 V2.1 · 正式发布）

- UI 刷新链补强：以 world revision 标记当前面板渲染版本；推演提交后即使漏掉一次 render，重新打开悬浮球 / 页面恢复 / 自愈检查时也会比较 revision 并从最新 store 重渲染。黄点同时承担“未读世界版本”提示，真正打开最新版本后才视为已读。
- 悬浮球新增「贴边收纳」：默认关闭；开启后自动吸到最近的左右屏幕边缘，悬浮球正好一半在屏幕内、一半藏到屏幕外；黄点仍可见，拖动时会先完整露出。
- 全局设置顶部改为整体 sticky：标题、关闭按钮和「常用 / 正文注入 / 连接与模型 / 高级维护」一起冻结在设置滚动容器顶部。
- 伏笔新增独立归档层：resolved / discarded 仍保留真实生命周期，归档只改变 archived 状态，不会把“已经发生/已经解决”的历史改写掉。记忆页新增「已回收 / 已归档」筛选。
- 伏笔关联扩展为 people / events / locations 多关联；UI 可按人物、事件、地点筛选，并在卡片上显示关联标签，不再把伏笔硬塞给唯一人物。
- 手动删除事实 / 伏笔 / 经历会写入记忆 tombstone；分支恢复、旧快照或后续整理不会让已经删除的旧记忆诈尸。此次为真实持久化结构升级，state schema 22 → 23，升级前继续自动留恢复点。
- 连接新增「酒馆已存方案」：直接引用 SillyTavern Connection Manager 已保存方案的 ID；Key、地址、模型、代理与 Secret 仍由酒馆保管，世界背面不复制。酒馆里更新方案后，下次推演自动读取新值；方案被删除则明确报错，不会偷偷换成别的连接。
- 模块 API 分流也可直接选酒馆已保存方案；世界推演、人物观测、长期记忆/历史、舆情都可以各自引用不同的酒馆方案。
- 普通「独立 OpenAI 兼容接口 → 经酒馆转发」改走 SillyTavern 的 Custom OpenAI-compatible dispatcher，不再伪装成 OpenAI Reverse Proxy；修复部分公益站出现 Unauthorized / Not Found 的兼容问题。DeepSeek V4 特殊兼容路径继续保留。
- 独立接口错误诊断新增 authorization / not-found 分类；最近 API 请求时间线同时记录安全的方案名与模型名，不记录 URL / Key。
- 后台 API 调度收口：同一个物理独立接口（传输方式 + URL + Key + 模型一致）现在共用单通道队列，世界推演 / 记忆 / 舆情 / 公共影响不会再同时撞同一路线。
- 新正文到来时不再为了“主线优先”直接 abort 已经发出的记忆/舆情/公共影响请求再马上重发；已经发出的批次先安全收尾，世界推演排队接棒，真正切聊天/重置时仍会硬取消旧任务。
- 主世界推演顺手生成本轮 L0 单轮记忆摘要；正常自动推演覆盖到的正文不再立刻由独立记忆 API 再读一遍。记忆有旧 backlog 时仍会分批补齐，不会跳过历史。
- 自动记忆在当前批次完成后发现世界推演排队，会先让路，不继续做下一批/rollup。
- 公共后处理统一为一条链：有待传播影响时先做公共影响，再只唤醒一次舆情；没有影响时才直接检查舆情。移除推演完成后的重复舆情 timer。
- 诊断新增最近 60 秒 / 5 分钟 API 请求计数、按任务分类和最近 20 条请求时间线，方便直接判断 429 是哪类任务打出来的；不包含 URL / Key。
- 手机端任务控制改为「推演世界 / 停止全部后台任务」同一行并排，底部导航恢复只显示 8 个板块；同时撤回前两轮会挤坏玲七聊天区的 grid 修补。
- 玲七六张姿态图改为插件内嵌 data URL，避免部分手机 WebView 无法解析扩展 `import.meta.url` 相对资源导致小猫显示破图。
- 当前技术版本更新为 `2.1.0`；对外版本名统一为「小猫版 V2.1」，不再显示旧的候选版本名。
- 玲七成为世界背面的常驻小猫管家：插件答疑、状态诊断、低风险代办、记忆整理、导演小纸条、问题分流与安全求助全部整合进同一套交互。
- 玲七会先判断问题属于用户设置、第三方/API、插件自身还是暂无法归因；只有真正需要插件作者介入时才生成给妈妈的小纸条，并先概括问题类型、现象、已检查内容和升级原因。
- 世界推演同步输出预算与截断保护：复杂 JSON 可继承更高输出上限；未闭合/截断结果不提交人物、事件、记忆或世界时间。
- 后台写状态任务之间使用安全排队；记忆整理等任务运行时仍可请求世界推演，但不会并发抢写。
- 正文注入独立成统一页面，可分别控制世界时间、背景、人物、暗流/环境、回声、事实、长期记忆与舆情是否递给正文；关闭注入不停止后台运行。
- 玲七姿态改为回复语义和真实任务状态驱动，取消随机幻灯片式换图；纯姿态变化只更新猫自身 DOM，不打断输入，不重建聊天和纸条。
- 设置页重构为「常用 / 正文注入 / 连接与模型 / 高级维护」，模块设置只保留模块自己的运行参数；说明文案改为玲七的小猫认知，但技术名称和危险操作保持清楚。
- 设置折叠标题支持两行说明，世界时间注入改为统一三态分段选择器，修复长文案溢出和窄屏排版。
- 数据 schema 不因品牌版本升级而强行跳号；旧存档迁移仍按真实数据结构需要执行，避免为了“2.0”制造无意义迁移风险。

## 1.8.0-rc.9（设置小字完整显示 / 世界时间控件重做）

- RC.8 为了防溢出把设置折叠标题说明强制成单行省略，导致玲七的小字经常只能看到半句；RC.9 改为最多两行自然换行，卡片宁可稍微长一点，也不随手砍掉说明。
- 「世界时间」注入方式不再使用三个独立方按钮，改为一个完整的三态分段选择器：完整 / 最小锚点 / 关闭。
- 宽屏下每个时间模式带一句简短解释；窄屏只保留主标签，避免小卡片被说明挤乱。
- 时间卡改为上下结构，玲七的说明在上、三态选择器在下，避免标题被挤成“世界时 / 间”。
- 仅修改设置 UI 和可读性，不改变世界钟与注入逻辑。

## 1.8.0-rc.8（设置折叠说明防溢出）

- 修复「正文过滤」等设置折叠标题的玲七说明过长时，文字横向溢出卡片的问题。
- 不删玲七文案；统一给设置组 / 子设置组的 `<summary><small>` 增加收缩宽度、单行省略和右对齐约束。
- 标题、说明都显式允许在布局中收缩，箭头区域不会再被长文案挤出卡片。
- 手机端进一步缩小说明文字可占宽度，避免窄屏再次穿出边界。
- 仅修 UI 布局，不改设置语义、世界状态或 API 行为。

## 1.8.0-rc.7（玲七设置文案增强猫味）

- 保留 RC.6 的第一人称设置文案，但进一步把“人类助手口吻”改成真正的小猫认知：叼到镜头边、压在爪爪下面、蹲着听风声、守着世界钟、把记忆塞进盒子等。
- 功能名称继续保持专业清楚，猫味只出现在解释、状态与提示，不把 `正文注入 / Token / API / 重置` 等核心概念改成难懂黑话。
- 全局设置标题改为「玲七的小窝」；常用项改成“放在爪边 / 收进里面”的逻辑。
- 正文注入页更明确使用“叼到镜头边 / 压在爪爪下面”表达前台可见性，同时继续强调“不注入 ≠ 不运行”。
- 人物、暗流、舆情、记忆各模块改成更明显的猫行为视角：盯人、顺爪捞进推演、竖耳朵听风声、把记忆纸片压好等。
- 世界书导入提示强调“先把真正的人叼出来，不把整个组织当成人”，保持现有安全判定逻辑不变。
- 高级维护继续保持危险操作说明清楚，同时用猫的理解方式表达：不乱碰事实、重置会把当前聊天箱子倒空、不会把旧世界藏床底下再让数据诈尸。
- 没有新增设置项、没有修改世界状态或 API 行为；本版仍然只是 RC 文案与可读性收口。

## 1.8.0-rc.6（设置页改为玲七视角）

- 保留 RC.5 的设置页信息架构，不撤回 `常用 / 正文注入 / 连接与模型 / 高级维护` 四页结构；这一版主要把被整理得过于“产品后台”的说明文案重新交给玲七。
- 功能名继续保持清楚：正文注入、世界时间、人物状态、Token、API、重置等技术名称不改成猫猫黑话。
- 解释、状态、提示和维护提醒改为玲七第一视角，例如“这个只是我家门口的小入口”“不递给正文看，我也会继续记着他们怎么生活”“这个是真的清空哦”等。
- 正文注入页由玲七直接说明核心概念：世界背面的内容仍在运行，用户只是在告诉她哪些需要递给正文看。
- 各板块的“正文可见”状态卡改成“我递给正文”，状态使用“会递过去 / 先留在后台”，但仍明确链接到统一的正文注入设置。
- 人物、暗流、舆情、记忆的模块说明改成玲七自己在做什么：照看人物、推演世界、听风声、整理记忆；API 路线与后台行为语义不变。
- 高级维护也使用玲七视角，但危险操作不卖萌弱化：真正重置仍明确写出会清空哪些数据，以及正文/API 配置不会被碰。
- 没有新增设置字段、没有改世界运行、注入、记忆、舆情或 API 逻辑；纯 UI 文案收口。

## 1.8.0-rc.5（设置页信息架构整理预览）

- 全局设置改为四个一级页签：`常用 / 正文注入 / 连接与模型 / 高级维护`。用户先选“想做什么”，不再面对一长串跨模块折叠组。
- 「常用」只保留真正的全局日常项：界面明暗、字号、悬浮球、世界引擎总开关、自动运行总开关。
- 「正文注入」成为独立页面，统一展示世界时间、世界背景、人物状态、暗流/环境、回声/后果、世界事实、长期记忆、舆情八类前台信息源。页面明确强调“不注入 ≠ 不运行”。
- 世界时间继续保留 `完整 / 最小锚点 / 关闭` 三态；其他信息源使用独立开关。
- 正文显露度与显露时机一起归入「正文注入 → 显露策略」，避免继续散落在暗流设置里。
- 「连接与模型」继续管理默认连接、独立接口、API 方案与模块 API 分流；模块只选择路线，不重复保存 URL / Key。
- 「高级维护」继续承载生成限制、标签过滤、恢复点、缓存/重置和诊断等低频/危险操作。
- 人物、暗流、记忆、舆情、此刻各板块的 ⚙ 设置顶部新增「正文可见」状态卡；只显示当前是否注入，并提供「修改」跳转到统一正文注入页，不再各放一份重复开关。
- 暗流设置移除正文注入总开关、七个注入开关和自动运行总开关，只保留世界脉搏、运转强度、触发频率、推演连接和高级失败/要求等真正属于世界运行的内容。
- 这版只整理设置入口与 UI 所有权，不新增世界状态字段、不改变后台推演/记忆/舆情实际运行逻辑。

## 1.8.0-rc.4（玲七姿态改为回复语义驱动）

- 撤掉玲七原来的随机待机姿态轮播。玲七不再每隔几秒从 idle / watch / note / confused / happy / hold 里随机换图，不再像播放幻灯片。
- 玲七回复完成后会持续保持这条回复对应的 `mascot_state`，直到下一条回复、真实运行状态变化，或用户主动点击玲七产生一次短暂互动反馈。
- 思考中仍是 `confused`，真实错误仍是 `hold`；这些属于任务状态，不是随机动画。
- 强化玲七提示词：`mascot_state` 必须按本次回复实际语义选择，明确禁止为了“显得活泼 / 轮换图片 / 制造变化”随机选姿态。
- 六种姿态重新明确语义：普通陪着=idle；认真回答/观察=watch；方案/记忆/小纸条=note；不确定=confused；顺利完成/开心=happy；拒绝/担心/失败=hold。
- 低成本本地管家命令也不再全部固定 `watch`：设置成功偏 `happy`，停止/阻止偏 `hold`，整理记忆偏 `note`。
- 对需要实际执行的管家动作，最终姿态会参考真实执行结果：模型回复前猜的是一种姿态，但动作实际失败时会改为 `hold`；记忆真正开始整理时为 `note`；明确成功时可为 `happy`。
- RC.3 的“猫咪动作只更新自身 DOM、不打断输入”继续保留；本版只是取消随机轮播并强化语义状态，没有新增世界/API 功能。

## 1.8.0-rc.3（玲七动作不再打断输入）

- 修复用户正在玲七输入框打字时，玲七随机换姿态或点击反馈动作会重建整个玲七界面，从而打断输入法组合态、光标和正在输入内容的问题。
- 这次不再继续给 textarea 做“重绘后恢复”补丁，而是直接修根因：玲七的纯视觉姿态变化现在只更新 `.wb-lingqi-mascot` 自己的 class、图片和状态，不再调用整页 `render()`。
- 随机待机动作、点击玲七后的临时反应、动作结束恢复，都走 mascot-only DOM 更新；聊天记录、小纸条、输入框、滚动容器完全不重建。
- 模型真正进入思考/错误、聊天内容变化、纸条状态变化等真实数据变化仍可以正常整页渲染；RC.2 的小纸条折叠状态保护继续保留，作为真实重绘时的兜底。
- 随机姿态结束后会自行重新安排下一次待机动作，不依赖整页重绘维持动画循环。
- 这属于 RC 交互修复，没有新增世界状态、记忆或 API 功能。

## 1.8.0-rc.2（玲七小纸条展开状态修复）

- 修复玲七随机换姿态、被点击、思考状态切换等触发界面重绘时，聊天里的“实际推进记录”、待确认小纸条以及右侧已贴小纸条会自己收起的问题。
- 根因是玲七姿态切换沿用整块 UI `render()`，原生 `<details>` 的 `open` 状态没有进入 UI 状态层，DOM 重建后自然回到默认关闭。
- 现在三个位置都有稳定折叠键：`message:<id>`、`pending`、`note:<id>`。每次重绘前先读取用户当前真实的展开/收起状态，再交给新的 DOM 恢复。
- 用户主动收起也会被记录；不是“只记展开不记收起”。
- 折叠状态只属于当前聊天的界面状态，真正切换聊天时会清空，不写进世界事实、角色记忆或玲七长期数据。
- 没有为了修这个问题停掉玲七随机姿态动画；猫照样会动，但不能再顺手替用户合上纸条。

## 1.8.0-rc.1（功能冻结 / 正式发布候选）

- 从 `1.8.0-alpha.6.5` 冻结功能进入 RC。后续 RC 阶段只修 bug、兼容、数据安全和主流程问题，不继续扩功能。
- 修复后台任务运行时左侧「推演世界」被粗暴禁用的问题：按钮现在仍可点击，若记忆整理、历史回溯、世界脉搏、公共影响、事实纠错、舆情、观测、玲七或其他后台操作仍在运行，则只创建本地排队，不并发请求、不抢写世界状态。
- 排队按钮会显示「推演已排队」；当前任务结束后自动开始。右侧「停止全部后台任务」可以连排队中的世界推演一起取消。
- 排队请求绑定 `chatToken + dataEpoch + contextEpoch`。切换聊天、重置当前聊天、清理运行时或全局停止时旧排队会失效，不能在新世界里诈尸。
- 若排队期间自动/其他推演已经把当时待同步正文追平，手动排队会识别“已经追上”并直接结束，不重复打第二次 API；若用户点击时本来没有待同步层，则保留“明确重新推演最新正文”的手动重跑语义。
- UI 版本标识改为按版本自动显示「候选版 / 测试版 / 正式版」，RC 不再被错误标成正式版。
- 新增 RC 发布前审计回归：旧状态迁移、恢复点完整性、事实权威、零耗时不推进事件、玲七旧聊天迁移、危险工具隔离、重置边界、旧异步写入失效、输出截断零提交、删除墓碑与双停止职责。
- 当前自动测试基线：417 项全部通过；仍需在真实 SillyTavern 环境完成最后一轮桌面/手机 smoke test 后再决定是否升 `1.8.0`。

## 1.8.0-alpha.6.5（玲七同步正式 dev.8 输出预算修复）

- 以 alpha.6.4 玲七问题分流版为底座，同步正式线 `1.7.3-dev.8` 的世界推演输出预算修复；玲七管家、问题分流、小纸条、记忆工具和现有 UI 全部保留。
- 世界推演模块留 `0 = 继承` 时，首轮现在直接继承模块手动上限或全局 Token 上限，不再在复杂深度推演中先缩到 4.6K / 重试 6.4K 才逐步放大。
- `max_tokens` 仍只是 ceiling：模型写完会自己停，不会强迫吐满；人物观测、历史/记忆、舆情等小任务继续维持自己的预算。
- JSON 截断识别同步 dev.8：支持识别 ````json` 围栏内未闭合的 JSON，并标记为 `output-limit / OUTPUT_TRUNCATED`。
- 世界推演因输出上限截断时，未闭合结果会被安全丢弃；本轮人物、事件、记忆和世界时间均不提交，世界保持待同步，可提高世界推演/全局 Token 后重试。
- 玲七帮助知识同步新规则：遇到已明确识别的 output-limit，会先教用户调整「高级与维护 → 生成限制」，并说明截断轮未提交数据，不会误当插件数据损坏后写纸条找妈妈。

## 1.8.0-alpha.6.4（玲七问题分流 / 妈妈反馈摘要）

- 玲七不再使用“答不上来 → 直接丢给妈妈”的单一逻辑。插件问题咨询新增结构化 `triage`：先判断由用户自己解决、由接口/第三方处理、玲七已经解决，还是确实需要妈妈介入。
- 分流维度包括责任归属 `user / provider / mama / unknown` 与问题类型：使用方式、设置/配置、API/中转、后台任务、记忆、人物、正文注入、舆情/新闻、世界书导入、数据一致性、界面/交互、兼容性、性能/限流、暂未归类。
- 明确的用法/设置问题不会写给妈妈：玲七必须给用户可执行的检查/设置步骤；模型即使误判为 `mama`，提交层也会降级为 `self_service`。
- 明确的 401/403/429、额度/限流、中转/上游模型不可用等第三方问题优先分流到接口/服务方；除非同时存在“插件按钮/解析/保存/状态不一致”等插件症状，否则不会占用妈妈的反馈。
- 如果玲七刚刚已经成功代办了一个低风险操作，也不会一边说“弄好了”一边又写纸条找妈妈。
- 只有插件行为明显和说明/设置不一致、数据删除/复活/覆盖/串聊天、内部任务前置正常却不触发、稳定复现的 UI 交互异常，或检查后仍无法归因，才允许 `triage.route=mama`。
- 给妈妈的小纸条现在必须先写人话摘要：`问题类型 / 一句话现象 / 玲七已经检查 / 为什么需要妈妈看 / 用户原话`。技术诊断被放到分隔线之后，妈妈不需要先读一堆状态/代码才能知道这是哪类反馈。
- 玲七聊天里的求助消息也会直接显示一个小型问题摘要卡：能看到类型、一句话和玲七查过什么，再决定是否点「接过小纸条」。
- `needsAuthorHelp` 继续保留旧数据兼容，但新回复真正是否升级给妈妈由 `triage.route` 决定。
- 自助/第三方问题若模型给出了 `triage.next_step`，插件会确保把“可以先试”的具体步骤带给用户，避免只做分类不解决问题。

## 1.8.0-alpha.6.3（玲七给妈妈的小纸条）

- 玲七正式记住自己的创造者称呼：做出插件、也是做出玲七的人只叫「妈妈」。玲七的管家提示明确禁止用“作者 / 开发者 / 维护者 / 程序员 / 写代码的人”等称呼代替。
- 玲七遇到自己确实查不明白的问题时，会说需要去问妈妈，并写一张给妈妈的小纸条；不再出现“去找那个写代码的人”这种出戏说法。
- 原「复制给作者」按钮改名为「接过小纸条」。功能仍是复制安全诊断信息，但交互语义变成从玲七爪子里接过她写给妈妈的求助纸条。
- 「接过小纸条」按钮重做为轻量圆角纸条按钮：加入 CSS 绘制的小纸片图标、轻微浮起反馈和更柔和的边框/阴影，不再像裸露的调试按钮。
- 复制成功提示改成「小纸条叼给你啦～拿去给妈妈看。」；复制内容标题改成「玲七给妈妈的小纸条」。
- 内部字段 `needsAuthorHelp` / `lingqi-copy-support-pack` 暂时保持兼容，不影响旧聊天数据迁移；用户界面与玲七可见语言全部改为妈妈/小纸条语义。

## 1.8.0-alpha.6.2（玲七记忆整理工具补线）

- 修复“让玲七帮忙整理记忆，她会答应但实际没有启动任务”的问题。原因是 alpha.6 管家动作白名单里漏掉了真实的记忆整理工具。
- 新增 `organize_memory` 管家动作，直接复用插件现有 `scanStoryMemoryHistory()` 长期记忆整理流程，不另造一套记忆逻辑。
- 明确区分“整理记忆”和“重建世界历史”：用户说“帮我整理记忆 / 帮忙整理一下记忆 / 归档记忆”只会启动长期记忆整理，绝不会误触发重型世界历史回溯。
- 明确的“整理记忆”命令走本地快路径，不需要先让玲七模型猜一次，因此少一次不必要 API 请求。
- 用户已经明确下达“整理记忆”指令时，不再弹第二次确认；但“手动整理记忆前自动保存”的恢复点照常建立，数据安全不缩水。
- 如果记忆系统关闭、已有记忆任务正在跑、世界推演/结算正在写状态、当前聊天没有正文，玲七会直接说明原因，不会硬开任务。
- 如果记忆本来已经追到最新且没有可压缩层级，玲七会直接告诉用户“已经整理到最新”，不制造空请求。
- 若 `organize_memory` 来自复杂自然语言的模型工具调用，会等玲七自己的回复请求先正常收尾，再启动记忆整理，避免两个任务互相抢占 busy 状态。

## 1.8.0-alpha.6.1（玲七空纸条区收起来）

- 右侧“小纸条”在没有任何可见纸条时不再强行撑满整列，只保留标题和一个很轻的猫猫空状态。
- 空状态正文高度压到约 68px；整个空纸条卡会自动收成一小块，不再留下半屏灰色空仓库。
- 一旦出现活跃/暂停纸条或已完成/过期纸条，面板会自动恢复完整高度和原来的独立滚动能力。
- 手机端同样允许空纸条卡自动收缩，不强行占满预留区域。
- 其余 alpha.6 全能小猫管家功能与左侧聊天布局均不改变。

## 1.8.0-alpha.6（玲七全能小猫管家 v0.1）

- 玲七的主定位从“导演猫”扩展为插件第一自助入口：继续能闲聊/写导演小纸条，同时开始负责回答插件用法、查询当前世界状态、排查常见问题和代办低风险面板操作。
- 常见问题和明确的可逆命令先走本地快路径，不额外打模型请求：当前版本、当前世界时间、正在运行的后台任务、常用注入/记录开关状态，以及“关掉人物注入 / 不要记录我 / 时间用最小锚点 / 停止推演 / 停止全部后台任务”等明确指令可以直接处理。只有需要自然语言解释、复杂判断、闲聊或导演理解时才调用玲七模型。
- 新增本地帮助知识库 `lingqi-help.js`。按用户问题只挑最相关的说明片段给玲七，不会每次把整本说明书塞进 prompt；知识覆盖世界背面概念、推演、模块化注入、玩家角色记录、人物、暗流/回声、记忆、舆情、世界书、缓存/重置、事实纠错、API 200/429、Token/超时和玲七自身。
- 每次对话会给玲七一份经过裁剪的实时状态：当前版本、世界时间、推演/记忆/舆情任务状态、注入开关、玩家记录、后台人物强化、人物/事件摘要、数据数量和安全脱敏后的最近 API 状态。不会把 API Key、接口 URL 或正文整段塞进去。
- 玲七现在遵守“知道就回答，不知道就承认不知道”：只有内置帮助/实时状态能支撑的结论才允许当成插件事实；看不出来时返回 `needs_author_help`，不准为了显得全能瞎编。
- 未解决的玲七回复会出现「复制给作者」按钮。点击时即时生成安全求助包，包含用户向玲七提出的问题、玲七卡住的原因和现有安全诊断；不包含 API Key、接口地址、聊天正文、角色身份锚点或自定义提示词。
- 求助包不直接存进玲七聊天历史，只保存一个很小的“需要作者帮助 + 原因”标记；真正诊断信息点击时再生成，避免聊天状态长期膨胀。
- 新增低风险管家动作：开关/调整正文注入、时间注入、世界/记忆/舆情自动运行、记录玩家角色、强化后台人物；开启/关闭明确 NPC 的后台推演；停止当前推演；停止其他全部后台任务；显式调用已有事实检查。
- 玲七暂时不会代办高风险动作：删除人物/记忆、重置当前聊天、修改 API、修改世界事实、改人物核心设定等仍走原有安全入口，避免“全能”变成“随便动数据库”。
- 玲七自己说“停止全部后台任务”时会保留正在回复的玲七请求，只停止其他后台任务；用户直接按全局停止按钮时仍会连玲七一起停止。
- 导演小纸条、六姿态猫猫、固定舞台、独立滚动区、随机待机动作等 alpha.5.x 现有功能全部保留。

## 1.8.0-alpha.5.5（玲七小纸条重新拉齐）

- 修正玲七页面里右侧“小纸条”面板整体下坠的问题。
- 在 alpha.5.3/5.4 中，为了让左侧聊天框贴住猫爪，聊天框被整体上提，但右侧小纸条没有同步处理，于是视觉上出现“一边高一边低”的歪斜感。
- 现在右侧小纸条也沿同一方向上提，和左侧聊天框重新落在同一条视觉基线上。
- 手机端同样同步这一对齐逻辑，只是上提幅度更轻，避免小屏下显得太挤。
- 不改变 alpha.5.4 的其他行为：聊天/纸条滚动位置保持、随机猫猫姿态、玲七紧凑头部、思考中困惑姿态等都保留。

## 1.8.0-alpha.5.4（玲七滚动修复 / 随机姿态 / 紧凑头部）

- 修复玲七聊天框在页面 rerender 后自动跳回最上方：现在会单独记录聊天区的滚动位置与“是否本来就在底部”。
- 如果用户本来就在聊天底部，新消息/状态刷新后会继续贴着最新消息；如果用户正在往上翻旧聊天，任何猫猫换姿势、状态条更新或玲七回复都不会强行把用户拖走。
- 右侧小纸条滚动位置也独立保存，玲七整个固定舞台仍然不参与滚动。
- 六张已有猫猫姿态全部重新参与待机：idle / watch / note / confused / happy / hold。空闲约 8.5～22 秒后随机换一个姿势，停留约 1.5～3.1 秒再回来，下一次延迟重新随机，不做机械轮播。
- 模型硬状态优先级修正：玲七正在思考时，无论此前随机到了什么或用户刚摸过猫，都立即使用 `confused` 问号姿态；请求失败使用 `hold`。
- 每次玲七新回复后会重置随机待机计时，先让模型根据当前对话返回的 `mascot_state` 正常展示一会儿，再进入随机小动作。
- 玲七页面顶部单独压紧：标题栏从通用 74px 收到 54px，状态条最小高度从 44px 收到 32px；只影响玲七页，不压其他模块。
- 手机端同步使用更紧凑但仍可读的玲七头部。

## 1.8.0-alpha.5.3（玲七更贴边地趴在聊天框上）

- 微调玲七固定舞台的视觉关系：左侧聊天框整体上提，让聊天框顶边更靠近玲七的小爪子。
- 顶部猫猫舞台同步轻微下压，所以现在视觉上更接近“玲七真的趴在聊天框顶上”，而不是悬在上面一截。
- 仅调整玲七视图的舞台间距，不改变 alpha.5.2 已完成的核心逻辑：外层不滚、聊天与纸条独立滚、思考中 `confused` 姿态、回复按 `mascot_state` 切姿态。
- 手机端也做了相同方向的较轻微重叠处理，避免小屏下压得过头。

## 1.8.0-alpha.5.2（玲七固定舞台 / 状态姿态）

- 废弃 alpha.5.1 的“点击后播放探头动画”方向；玲七不是点击触发的装饰动画。
- 玲七整个页面改为固定舞台：外层 `wb-view-content` 不再上下滚动，布局始终锁在当前可用高度内。
- 只保留两个独立滚动区域：左侧聊天记录、右侧小纸条内容。聊天输入框和小纸条标题都固定，不随内容滚走。
- 猫猫从聊天滚动容器中移出，放到独立的固定顶部舞台行；无论聊天记录向上/向下滚多少，猫都不会被消息顶走或滚出视野。
- 猫猫开始真正切换六张状态姿态素材，而不是永远固定 `idle.png`：模型请求进行中前端强制使用 `confused`（问号/困惑）姿态；失败时使用 `hold`。
- 玲七聊天 JSON 新增 `mascot_state`。每次回复可以根据这一次对话内容选择 idle / watch / note / confused / happy / hold；回复完成后界面立即使用该姿态。
- 活跃小纸条不再永久把猫锁成 note。姿态以“当前这次回复的情绪/动作”为准；只有模型正在思考等硬状态会临时覆盖。
- 手机端同样采用固定猫猫舞台 + 聊天/纸条两个独立滚动区，不再让整个玲七页参与滚动。
- 本版基于 alpha.5（已同步正式 dev.7）重新实现，没有携带 alpha.5.1 的错误探头样片代码。

## 1.8.0-alpha.5（玲七个人开发线 · 同步正式 dev.7）

这不是正式发布包。它把最新玲七 `1.8.0-alpha.4.3` 与正式开发线 `1.7.3-dev.7` 做了三方同步，供作者本人继续测试玲七。

- 保留玲七 alpha.4.3 全部现有功能：猫猫导演、小纸条、可读的人话版发展记录、独立聊天/纸条滚动区、最新趴框布局与六姿态资源。
- 同步正式 dev.1～dev.7 的全部本体更新：数据安全与上限修复、舆情单条删除/世界时间排序、强化后台人物、回声/NPC 删除防诈尸与回声时间修复、智能世界书人物导入、当前聊天缓存/一键重置、记录玩家角色开关、事实纠错与模块化正文注入、推演停止/全局停止分工。
- 玲七导演纸条与「强化后台人物推演」现在可以在同一次世界推演提示中共存，不会二选一覆盖。
- 使用玲七 alpha 的世界书/舆情调度与 prepared-store 架构，同时接入正式线的手动删除 tombstone、玩家角色记录策略、舆情单条隐藏和恢复安全过滤。
- 正式 dev.7 的「停止全部后台任务」已扩展认识玲七：玲七请求也会显示在正在运行任务里，并能被全局停止、清缓存和聊天重置正确取消。
- 清缓存时保留玲七已经写下的聊天与纸条，只清运行态；「重置当前聊天数据」仍会按设计清空整个当前聊天的世界背面数据，包括玲七聊天/纸条。
- 保留 alpha 舆情的时间/公开事件调度：普通 `saveStore` 不会因为正式线合并而绕过新闻时间门槛直接制造新闻；同时保留正式线的单条删除防复活与主世界时间排序。
- 样式采用真正三方合并，而不是只追加 `.wb-lingqi-*`：玲七增加的第 8 个导航页、世界背景排版与正式 dev.7 新设置样式均保留。

## 1.7.3-dev.7（正式线测试包）

- 原先重复的两个底部操作按钮拆分职责：侧边按钮只负责「推演世界 / 停止推演」；底部按钮固定为「停止全部后台任务」。
- 「停止推演」仍然只取消当前主世界推演，不碰记忆、舆情、观测等其他后台任务。
- 「停止全部后台任务」统一覆盖：主世界推演、排队推演、记忆整理、历史回溯、世界脉搏、公共影响传播、事实纠错、舆情刷新、随便逛逛和人物即时观测。
- 全局停止只取消尚未完成的工作，不回滚已经成功提交的世界状态；记忆整理已经保存的批次继续保留，正在生成的批次不会提交。
- 全局停止会清掉当前排队的世界推演任务并提高 `dataEpoch`，防止已经排队但尚未开始的旧任务随后继续写入。
- 当前没有后台任务时，全局停止按钮保持禁用；悬停可看到当前正在运行的后台任务名称。
- 本包继续属于 1.7.3 正式开发线，不包含玲七 alpha 功能。

## 1.7.3-dev.6（正式线测试包）

- 正文注入改为模块化：总开关下可分别控制世界背景、人物状态、进行中事件/世界环境、回声/已结算后果、世界事实、长期记忆和舆情。关闭某项只是不递交正文，对应后台模块仍继续存在、推演和保存。
- 世界时间注入改为三档：完整 / 最小一致性锚点 / 关闭。「最小锚点」只防正文无因果倒退、跨日或跳回旧日期，不要求正文主动显示时间。
- 原「记忆注入正文」独立开关收拢进统一正文注入面板；记忆系统运行开关仍独立存在。
- 建立世界事实来源优先级：作者/手动事实 > 正文明示事实 ≈ 已结算事件结果 > AI 后台推断。低权威 AI 推断不能再无声覆盖更高权威事实，冲突会保留到 consistencyConflicts / audit 中。
- 新增「检查并修正世界状态」：用户主动触发一次 AI 一致性检查，对照最近正文、人物作者锚点、世界背景和手动事实，只处理能给出逐字证据的矛盾。
- AI 纠错采用提交层验证，不是让模型直接任意改数据库：证据必须真实存在于指定来源；可修世界事实、长期事实和有限的动态人物状态，不能改人物身份/性格/背景/外貌。
- 对明确由错误事实衍生、且尚未终结/尚未递交正文的暗流，纠错结果可安全撤销；已经 resolved / cancelled / missed 或 delivered 的事件绝不删除，因此自动纠错不能成为“正文时间机器”。
- 事实纠错不会每轮额外调用 API。日常写入先由本地权威等级自动拦截，只有用户点「检查纠错」时才额外发起一次检查请求，避免为了自纠错增加常驻 API 压力。
- 本包继续属于 1.7.3 正式开发线，不包含玲七 alpha 功能。

## 1.7.3-dev.5（正式线测试包）

- 人物设置里的「描写玩家内心」改为真正的「记录玩家角色」开关。
- 默认保持开启以兼容现有用法：开启时，世界背面只记录 user 在正文中已经明确发生的位置、行动和客观状态；不再提供记录玩家 inner_voice 的开关，玩家内心始终由玩家自己决定。
- 关闭「记录玩家角色」后，user 不再作为人物实体保存到人物板块，也不会进入后台人物推演、世界脉搏人物上下文、公共影响人物更新或正文人物状态注入。
- 关闭后不会抹掉玩家已经造成的世界事实：正文中真实发生的行动、关系变化、物品变化、地点影响、事件后果与长期记忆仍然正常结算。
- 旧聊天里已经存在的 user 人物卡会在关闭开关后立即从当前状态与初始状态中移除；旧分支快照、恢复路径和后续保存也统一经过同一过滤策略，不会把玩家卡再次复活。
- 模型即使在关闭状态下返回 `is_user=true` 的 `people_upsert`，插件也会在提交层直接忽略，避免仅靠提示词约束。
- 玩家卡开启记录时也会强制清空 `innerVoice`；旧版曾保存的玩家内心不会继续显示或注入。
- 本包继续属于 1.7.3 正式开发线，不包含玲七 alpha 功能。

## 1.7.3-dev.4（正式线测试包）

- 「高级与维护 → 数据维护」新增「清理当前聊天缓存」：清理人物即时观测缓存、闲逛舆情缓存、世界书扫描结果、模型列表、运行中/排队任务和临时状态；人物、事件、回声、记忆、世界钟、正式舆情和 API 配置均保留。
- 新增「重置当前聊天数据」：一键清空当前聊天的世界状态、人物、事件、暗流、回声、记忆、舆情、观测缓存、恢复点、手动删除记录、聊天级 memory archive 与 branchOverrides。
- 真正重置会同步删除聊天消息 `extra` 与各 swipe 中保存的世界背面分支快照，防止旧分支/重抽/恢复路径把已清空数据重新抬回来。
- 重置不会删除或修改聊天正文，不会改动全局插件设置，也不会动 API URL、Key、模型、连接方案或 Token / 超时设置。
- 重置前不会自动创建恢复点；操作采用两次确认，明确提示这是不可从世界背面恢复的干净重测操作。
- 新增运行时 `dataEpoch`：重置后，重置前已经排队的正文推演任务会自动失效；正在运行的后台任务也会被取消，避免旧请求在重置完成后把旧状态重新写回来。
- 「清理缓存」与「重置当前聊天」都不会主动清除真实 429 路由冷却；限流冷却属于 API 保护，不作为“脏缓存”误删。

## 1.7.3-dev.3（正式线测试包）

- 重做世界书人物导入的默认流程：选择世界书后可直接点「一键智能导入人物」，不再要求先扫描、逐条勾选再导入。
- 自动识别会跳过 MVU、变量初始化、JSONPatch、正则、脚本、前端/状态栏等技术配置条目，避免把变量文件误当人物卡。
- 支持“人物和势力/组织设定写在同一条世界书内容里”的常见格式：会尝试从同一条内容中拆出多个独立人物，再分别导入人物板块；来源势力/父条目会保留到人物背景信息中。
- 支持从重复「姓名 / 角色名 / Name」字段、`<character>/<npc>/<person>` 结构，以及「角色：XXX / 人物：XXX」标题块中拆人物。
- 自动导入只处理置信度足够的人物；不够确定的候选会留在高级手动挑选里，不会为了“一键”把地点、国家、规则或普通势力条目硬塞成人物。
- 原来的搜索、筛选、逐条勾选功能保留为「高级手动挑选」，用于特殊格式和自动识别兜底。
- 混合条目在高级列表中会明确显示「从混合条目拆出 / 混合设定 / 技术 / MVU」等状态，非人物条目不可误勾选。
- 本包继续属于 1.7.3 正式开发线，不包含玲七 alpha 功能。

## 1.7.3-dev.2（正式线测试包）

- 修复“回声删了又回来”：删除终局事件时会在聊天级存储中留下持久删除标记。旧分支快照、恢复点、异步保存再次加载时都会经过删除过滤，不再把已删回声从旧状态抬回来。
- 修复“人物删不掉 / 删除后又出现”：用户明确删除的人物会记录稳定删除标记（ID / 姓名 / 世界书引用）；旧快照恢复或模型后续再次 upsert 同一人物时都会被过滤。只有用户手动重新加入，或明确重新从世界书导入，才解除删除标记。
- 人物删除同时清理对应观测缓存；删除动作现在明确返回成功，编辑窗可以正常关闭。
- 手动撤销会同时恢复删除标记，因此“删除后撤销”仍然有效。
- 修复回声时间错乱：到期的 duration / scheduled 事件形成 resolved / missed 结果时，默认按原 `dueAt` 记录发生时间，而不是把跨天批量结算的多个事件全盖成当前结算时间；模型显式提供 `resolved_at` 时仍以显式时间为准。
- 兼容旧数据：回声界面与排序也会用 `dueAt` 修正旧记录的显示时间，因此已经存在的“多条回声挤在同一时间”无需重新生成。
- 本包继续属于 1.7.3 正式开发线，不包含玲七 alpha 功能。

## 1.7.3-dev.1（正式线测试包）

本包从正式 1.7.2 数据安全热修线继续开发，不包含玲七 / 猫猫 / 小纸条等 1.8 alpha 功能。

- 保留 1.7.2 数据安全热修：人物持久存储不再被 36 人上限静默裁掉；移除插件 16K Token 人为封顶；世界钟支持最高六位年份与大年份日期运算。
- 舆情新闻与论坛支持逐条删除。删除只隐藏传播层卡片，不删除世界事件或人物记忆；普通保存/世界新闻重新同步也不会把已删卡片直接复活。
- 修复舆情新旧混排：以主世界时间 `worldMinute` 作为第一排序键，现实生成时间只做同一世界时刻的次级排序。
- 新闻与论坛卡片显示对应主世界时间。
- 新增“强化后台人物推演”开关：把最逾期 NPC 直接并入已有的正文世界推演 / 世界脉搏请求，要求逐个完成生活结算，不再只围绕当前与玩家同场的人物更新。
- 修复人物生活结算的假完成：普通后台 `people_upsert` 不再无条件刷新 `lastLifeTickAt`；只有当前前台人物，或本轮明确被选为生活结算目标且模型实际返回的人物，才算完成本轮 life tick。
- 正式线暂不沿用 alpha.4.4 的“推演后自动再追一批 NPC”额外请求。考虑到 429 / 公益站限流，强化后台人物优先复用现有请求；统一任务调度器完成后再决定是否安全追加补齐批次。

## 1.7.2 data-safety hotfix

- P0：人物持久化取消 36 人总量裁剪；36 只保留为单次模型 payload / 任务 / 上下文预算。
- schema 升到 22，升级前继续自动留迁移恢复点。
- 解除插件自设的 16K Token 上限及 JSON 重试 16K 封顶。
- 世界日历支持 1—999999 年、正文识别 1—6 位年份，并改用纯整数公历日期算法。

# Changelog

## 1.7.x

### 1.7.2
- 「此刻设置」补上明确可见的「世界背景设定」入口。
- 世界背景未填写时显示引导；已填写时显示字数与简短预览，可一键进入原有世界编辑器。
- 继续使用同一份 `world.background` 权威数据，不复制世界观、不改变 schema，也不允许普通推演改写。

### 1.7.1
- 新增「全局设置 → 高级与维护 → 生成限制」：可设置 Token 上限与单次最长等待时间。
- 默认保持自动：世界推演约 180s、人物观测 120s、历史/记忆 300s、舆情/闲逛 150s；浏览器切到后台时暂停超时计时。
- 支持世界推演、人物观测、历史/记忆、舆情/闲逛四类模块单独覆盖 Token / timeout；0 表示继承全局。
- Token 设置改成真正的“最大上限”，不再因为用户填 8K 就强迫原本只需要 2K 的小任务申请 8K。
- 超时只计算单次模型请求的活跃生成时间；429 cooldown 与自动重试等待不会吃掉 timeout。
- 自定义 API 与跟随酒馆两条生成路线统一应用生成限制。
- timeout 统一分类为可诊断的 transient error，可按原有重试设置处理。
- 修复人物观测在低 Token cap 下“声称升到 8K、实际仍被 cap 截住”的假扩容。
- 修复 timeout 与 AbortError 的竞态，避免真实超时被误判成用户取消。
- 诊断信息新增本次请求原预算、实际 Token 上限、timeout 值与设置来源。

### 1.7.0
- 修复自动推演频率被一致性屏障绕过：每 2/3/5 轮等设置现在真正按累计轮数触发；手动模式也不会在下一次发送前偷偷开推演。
- 新增 NPC Life Tick：人物拥有独立生活结算时间。长时间未出镜的人会按世界时间进入到期队列，从自己的位置、目标、关系与状态继续生活，不再只围着最近正文转。
- 优化聊天楼层存储：提交后不再同时保留完整 base/result；当前 swipe 不再双写两份；超过最近 50 个 AI 楼层后，清理废弃旧 swipe 与诊断负担，同时保留当前旧分支的确认结果用于回滚。
- 记忆页新增批量管理：多选、选择当前显示、删除选中、清空当前筛选/全部未锁定记忆。批量删除前自动建立恢复点，锁定记忆默认保留。
- 修复手动删除经历后可能被 chat-level 摘要档案重新恢复的问题。
- 人物卡布局改为自适应列宽；人物姓名优先完整显示，地点与后台状态不再把名字挤成一个字。

## 1.6.x

### 1.6.1
- 新增人物认知防火墙：世界知道的幕后真相不会再自动灌进角色认知。
- `actors`、事件 `known_by`、前台显露度都不再能直接让 NPC “知道”事件。
- 新认知必须通过 `knowledge_updates` 提交获知路径、证据和角色实际知道/怀疑的版本。
- `inferred` 只能形成怀疑，不能直接升级成确认事实。
- 人物即时观测不再读取完整最近正文、GM 事件摘要或完整世界背景，只使用该人物自己的认知版本。
- 主世界推演生成的角色独白也经过认知校验；引用未知秘密会被拒绝。
- 镜头外 confirmed 获知需要可追踪的“获知过程事件”，不能拿“人物是受害者/参与者”当知情证据。
- 升级到 schema 20 时会保守清洗旧版 NPC 认知账本，避免历史全知污染继续残留。

### 1.6.0
- 新增「世界背景设定」：作为当前世界自己的长期基础设定，而不是全局插件设置。
- 世界状态引擎、World Pulse、公共事件影响、人物即时观测与闲逛都会参考这份背景。
- 正文一致性层会带入一份精简背景约束，避免正文随手违反世界底层规则。
- 普通推演只能读取背景，不能自动改写；背景只通过用户手动编辑更新。
- 当前世界卡只显示「世界背景已设定」提示，不把大段设定常驻堆在首页。

## 1.5.x

### 1.5.6
- 新增悬浮球显示开关；关闭后插件照常运行，并保留扩展设置恢复入口。

### 1.5.5
- 刷新世界舆情与「随便逛逛」彻底拆成独立任务，可同时运行。
- 两边按钮锁、状态提示与 single-flight 分离。

### 1.5.4
- 「随便逛逛」不再受主线推演 busy 限制，也不会被主线抢占。
- 闲逛保持非正史只读，不影响世界状态、人物认知和世界时间。

### 1.5.3
- 普通世界推演产生的公开新闻正式进入持久滚动新闻账本。
- 新推演不再把上一批新闻直接刷掉。

### 1.5.2
- 修复舆情刷新时 `mergePublicOpinionStream is not defined`。
- 新增跨模块 import 与未定义标识符回归检查。

### 1.5.1
- 世界事实区分当前、未来、历史和持续后果。
- `public_summary` 与公开终局 `public_result` 分离。
- 情境关联桥去重，并收窄到当前 user 意图与当前场景。
- 舆情刷新改成真正原子更新。

### 1.5.0
- 新闻改为连续滚动流，同一事件有后续时更新原新闻。
- 公开世界事件稳定落成世界事实。
- 新增情境关联桥：地点、组织、行业等世界现实会在真正相关时重新影响正文。
- 公共世界新闻开始按世界时间持续推进。

## 1.4.x

- 新增 World Pulse，让天气、交通、组织、行业、社会环境等镜头外世界持续运转。
- 新增长聊天世界回溯，可从已有聊天恢复世界时间、人物状态、事实与未完事件。
- 重做公开传播逻辑：私密剧情不会因为正文可见就自动变新闻。
- 加入 Public Impact，公开事件会继续影响人物、组织、地点、资源、行程等。
- 加强 429 冷却、single-flight 与失败不提交，避免重复任务和无脑重试。

## 1.3.x

- 建立权威世界事实层与一致性屏障。
- 加强人物后台状态、世界书人物保护、正文过滤、人物独白与头像。
- 重构设置与移动端界面。
- 强化前后台联动：后台结果会在真正相关时自然进入正文，而不是围着正文做一次性延伸。

---

从 1.3.0 到现在，世界背面逐步从“后台推演工具”变成一个持续存在、会自己变化、会留下后果，并且角色真正生活其中的世界状态引擎。
