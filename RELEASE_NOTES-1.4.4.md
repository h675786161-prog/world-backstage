# 世界背面 1.4.4 · 429 不许再把公益站放成烟花啦 (╬ Ò﹏Ó)

这版专门收紧所有后台任务的重试与并发安全。

## 429 现在是真正的“冷却”
- 优先读取上游 `Retry-After`
- 没有提示时使用保守的指数退避 + 小幅随机抖动
- 429 不会立刻重新请求
- 同一条 API 路线上的其他后台模块也会一起尊重冷却，不会换个模块名继续轰

## 同一个任务保持 single-flight
主世界推演、世界脉搏、公共事件影响、历史回溯、记忆整理、人物观测、舆情等后台任务都有稳定 task key。
同一个推演任务已经在跑 / 冷却 / 重试时，再次触发只会复用那一份，不会并发复制第二个实例。

## 拔掉一个真正危险的旧逻辑
以前主世界推演在“内部有限重试全部失败”之后，
队列 rejection handler 还会在 **40ms 后重新创建一轮推演**。

也就是说接口如果一直 429：
有限重试 → 整轮失败 → 40ms 后新建一轮 → 又有限重试……
理论上确实可能变成无上限的外层重试。

1.4.4 已删除这条自动重建：
失败后保持 pending / error，世界状态不提交。
只有下一次明确触发（新正文、用户操作、一致性屏障等）才会在冷却之后重新尝试。

## 失败仍然绝不提交
429、网络失败、空回、JSON 失败、截断都不会推进世界，
也不会提前启动依赖这份失败结果的后续公共影响 / 舆情链。

诊断信息新增：
- 当前活跃路线冷却数量
- 最长剩余冷却时间
- 当前 single-flight 任务数
- 上游 Retry-After（若接口提供）

不会记录 API Key 或真实接口地址。
