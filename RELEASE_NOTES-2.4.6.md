# 2.4.6 · 设置持久化保险

这版不只修标签规则，而是统一处理“UI 看起来改了，但关掉/重绘后恢复旧值”的同类问题。

- 标签过滤规则改为输入时即写入持久设置，不再依赖 blur/change。
- 所有 `data-wb-setting` 类型的持久设置在捕获阶段先落盘；原有 change 处理仍负责运行时副作用。
- 自定义等待秒数与四模块 Token/超时数字同步纳入即时持久化。
- 模块 API 分流与酒馆连接方案选择增加持久化兜底。
- 关闭总设置/模块设置前会先统一 flush 当前可见的持久设置。
- 页面切后台或 pagehide 前也会 flush，避免移动端 WebView 杀 DOM 后恢复旧值。
- 中文输入法 composition 期间不写半成品，compositionend 后再保存。

明确保持临时、不自动持久化：世界书/记忆/通讯搜索和勾选、未发送消息草稿。

明确保持“点击保存才提交”：API、生图 API、世界、人物、事件、记忆、纪事等编辑表单。
