# 世界背面 1.3.7 · 设置真的回家啦～

1.3.6 只是把旧总设置“搬进”各板块，结果连大框和小框一起搬了，手机上一层套一层，看着像俄罗斯套娃（捂脸）。

1.3.7 直接重做板块设置渲染：

- 此刻 / 人物 / 暗流 / 舆情 / 记忆都使用自己的扁平设置页；
- 不再出现“界面与显露 → 显露 → 设置项”这种旧总设置层级；
- 小节只用标题、留白和细分隔线，不再用大卡片包大卡片；
- 只有真正低频的项目才进一层「高级」；
- API 地址和 Key 仍然只在全局管理，各板块只选择自己使用哪条连接路线；
- 全局外观也直接显示明暗和字号，不再“界面与显露 → 界面”套两层；
- 底层 setting key 没有复制，设置仍然只有一份真实状态。

这次不是给旧框写 CSS 隐身术，是板块设置真的换了骨架啦 (｡•̀ᴗ-)✧
