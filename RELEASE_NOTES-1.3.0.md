# World Backstage 1.3.0

## 核心变化

- 世界背面正式以“权威世界状态”为中心，而不是把正文当成唯一事实源。
- 新增 `worldFacts` 世界事实结算层与 `consistencyConflicts` 一致性记录；状态 schema 升级到 14。
- 已结算后台结果会约束后续正文连续性；正文明确发生的新变化仍会回写世界状态。显露开关只控制结果是否主动靠近镜头，不能关闭权威世界事实本身。
- 世界钟会跟随正文新的明确日期自动校准；“时间与地点”栏中的同日更晚明确钟点也会直接同步，旧钟点不会反向拖回时间。
- 暗流 / 回声 / 纪事职责重新收口：未显露并归档的结果不再同时留在回声。
- 舆情支持模块内自动更新，并只在公开信息源实际变化时刷新。
- 设置页、记忆页与各板块说明整体瘦身，默认只保留必要使用信息。
- 独立 API 修复 HTTP 200 包裹上游 429/额度错误的识别与诊断。

## 兼容

- 1.2.x 旧存档会自动迁移到 schema 14。
- 旧人物位置/行动不会在升级瞬间被直接硬化为权威事实，以免把 1.2.x 已经存在的前后台错位锁死；首次成功的 1.3 推演会结合最新正文重新协调人物状态。终态事件可安全保留为世界事实，迁移不会凭空补角色认知或长期记忆。
- GitHub 正式入口仍为 `index.js`，不需要 `hotfix-entry.js`。
