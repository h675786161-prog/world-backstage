{
  "name": "world-backstage",
  "private": true,
  "version": "0.8.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check index.js && node --check core.js && node --check ui.js && node --check api.js"
  }
}
